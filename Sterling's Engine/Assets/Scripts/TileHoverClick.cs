using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TileHoverClick : MonoBehaviour {

    //Colors for the events (eventually will change this, to be a tint, rather than actually changing teh color)
    Color Highlight = Color.yellow;
    Color Cicked = Color.red;

    //Need to remember the Original Color (and later on, the original tint.)

	void OnMouseOver()
    {

    }

    void OnMouseExit()
    {

    }
}
